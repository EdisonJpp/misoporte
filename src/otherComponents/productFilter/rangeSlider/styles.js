import { StyleSheet } from "react-native";
import {
    windowHeight
} from "../../../theme/appConstant";

export default rangsliderStyles = StyleSheet.create({
    slider: {
        marginTop: windowHeight(20)
    },
})