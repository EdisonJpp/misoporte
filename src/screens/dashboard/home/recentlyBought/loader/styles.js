import { StyleSheet } from "react-native";
import {
    windowWidth,
} from "../../../../../theme/appConstant";

export default recentlyBoughtLoaderStyles = StyleSheet.create({
    view: {
        marginLeft: windowWidth(10)
    },
});