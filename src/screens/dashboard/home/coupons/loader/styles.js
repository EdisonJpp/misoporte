import { StyleSheet } from "react-native";
import {
    windowWidth,
} from "../../../../../theme/appConstant";

export default couponsLoaderStyles = StyleSheet.create({
    couponView: {
        marginLeft: windowWidth(10),
        borderRadius: windowWidth(10)
    },
});