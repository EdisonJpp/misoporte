import { StyleSheet } from "react-native";
import {
    windowHeight,
} from "../../../../theme/appConstant";

export default couponsViewStyles = StyleSheet.create({
    coupon: {
        marginTop: windowHeight(20)
    },
})