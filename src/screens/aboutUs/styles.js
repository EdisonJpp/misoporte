import { StyleSheet } from "react-native";
import {
    windowHeight
} from "../../theme/appConstant";

export default aboutUsStyles = StyleSheet.create({
    mainView: {
        height: '100%',
        width: '100%'
    },
    img: {
        width: '100%',
        height: windowHeight(240)
    },
})