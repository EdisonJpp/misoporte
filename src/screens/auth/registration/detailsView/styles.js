import { StyleSheet } from "react-native";
import Colors from '../../../theme/colors';

export default registrationStyles = StyleSheet.create({
    mainView: {
        backgroundColor: Colors.primary,
        flex: 1
    },
});