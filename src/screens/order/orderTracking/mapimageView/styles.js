import { StyleSheet } from "react-native";
import { windowWidth } from "../../../../theme/appConstant";

export default mapImageViewStyles = StyleSheet.create({
    img: {
        width: windowWidth(700),
        height: '60%',
    },
})